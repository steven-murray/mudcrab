# Copy to MOFAM-test/.env and source it before running scripts.

# Required for nexus:<game>/<mod_id>/<file_id> sources.
export NEXUS_API_KEY="replace-with-real-key"

# Optional override for local API mocking.
# export NEXUS_API_BASE="https://api.nexusmods.com/v1"
