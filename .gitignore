# Build output
/target

# Python venv (vestigial — the one script in MOFAM-test/scripts is stdlib-only)
.venv/
__pycache__/

# Machine-local tool configuration — contains host-specific paths
tools.toml

# Secrets
api-key.txt
.env

# MOFAM working area: generated artifacts, real mod binaries (hundreds of MB),
# and downloaded archives. The *inputs* (source markdown, TOML, notes) are tracked.
MOFAM-test/output/

# Run logs
*.log
